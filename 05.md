## Install Python 3.x and check the version via CLI.

So I installed python using homebrew first, but then once i had to manage different versions i decided to use pyenv and download the required version, and in case i have to create a virtual environment i use pyenv-virtualenv.

Simple as that. Although previously brew installation and pyenv installations was conflicting gloabally but somehow i managed that and now it's ok I think i am using the pyenv installed version globally.

Currently using - Python 3.14.2
